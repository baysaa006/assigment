{
  "name": "assigment",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "i-server": "cd server && yarn install",
    "s-server": "cd server && yarn start",
    "i-client": "cd client && yarn install",
    "s-client": "cd client && yarn start"
  },
  "workspaces": [
    "client/*",
    "server/*"
  ],
  "dependencies": {
    "zustand": "^4.4.7"
  }
}
